# SHOP-DEMO_bot

Telegram-бот для магазина (демо-версия), построен на Python и aiogram.
Бот работает в продакшне, использован Docker для удобного развертывания.


## Возможности
- Взаимодействие с пользователем через Telegram
- Обработка состояний с помощью FSM
- Асинхронная логика
- Работа с базой данных (PostgreSQL/SQLite)
- Модульная структура проекта
- Админ-панель с ролями "manager" и "super" (можно расширить количество ролей)
- Логирование действий админов с помощью Middlewares (можно добавить действия пользователя)
- Поддержка платежей

## Взаимодействия с ботом

- 💻 Меню пользователя:
    1. Команда "/start" показывает приветствие и навигационое меню по боту.
    2. Кнопка "Каталог" показывает все доступные категории и товары в них.
    3. Кнопка "Аккаунт" позволяет пользователю увидеть данные о себе и обновить их.
    4. Кнопка "Корзина" показывает все товары в ней и позволяет управлять его количеством, удалять и оформлять заказ.
    5. Кнопка "Мои заказы" позволяет посмотреть все свои заказы по их статусу.

- 🤖 Админ меню:
    1. Команда "/managers" доступна с роли "manager" и выше, показывает меню мендежера.
    Функционал: 
    - Просмотр заказов и перевод их в другой статус.
    - Возможность скрывать и показывать товар.
    2. Команда "/super" доступна с роли "super", показывает меню супер админа.
    Функционал: 
    - Просмотр списка админов.
    - Возможность назначить или снять менеджера.
    - Посмотреть действия админов.

## Технологии
- Python 3.12.3+
- asyncio
- aiogram
- PostgreSQL / SQLite
- SQLAlchemy
- Docker & Docker Compose

## Структура проекта

.
├── handlers/           # обработчики Telegram
├── services/           # бизнес-логика
├── states/             # состояния FSM
├── database/           # модели базы данных
├── keyboards/          # клавиатуры
├── middlewares/        # middleware бота
├── main.py             # точка входа
├── Dockerfile          
└── docker-compose.yml

## Запуск через Docker

1. Клонируйте репозиторий
2. Создайте `.env` на основе `.env.example`
3. Выполните команду:

```bash
docker compose up --build   # Собрать и запустить
docker compose down         # Остановить
```

## Статус
Бот работает в продакшне.
Можно протестировать здесь: [Бот](https://t.me/shopdemo2_bot)

## Примечания
Проект можно использовать как шаблон для создания Telegram-ботов
или расширять дополнительной бизнес-логикой.